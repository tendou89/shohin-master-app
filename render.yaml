services:
  - type: web
    name: shohin-master-app
    env: node
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: shohin-data
      mountPath: /var/data
      sizeGB: 1
